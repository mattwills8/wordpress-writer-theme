<?php
 if ( ! defined( 'ABSPATH' ) ) { exit; } if ( ! class_exists( 'ReduxFramework_extension_vendor_support' ) ) { class ReduxFramework_extension_vendor_support { static $version = "1.0.0"; public function __construct( $parent = null ) { if ( empty( $this->extension_dir ) ) { $this->extension_dir = trailingslashit( str_replace( '\\', '/', dirname( __FILE__ ) ) ); $this->extension_url = site_url( str_replace( trailingslashit( str_replace( '\\', '/', ABSPATH ) ), '', $this->extension_dir ) ); } include_once $this->extension_dir . 'class.vendor-url.php'; Redux_VendorURL::$dir = $this->extension_dir; Redux_VendorURL::$url = $this->extension_url; } } } 